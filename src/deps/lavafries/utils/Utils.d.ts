export default class Utils {
    static newTrack(data: any, user: any, load?: string): any;
    static newPlaylist(data: any, user: any, load?: string): any;
    static formatTime(ms: number): string;
}
